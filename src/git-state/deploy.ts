/**
 * Deploy flow: explanation gate, commit, tag, push.
 * Phase 0: stub. Implemented in Phase 1.
 */

// TODO(phase4): implement deploy flow with agent explanation gate
export type DeployStub = Record<string, never>;
