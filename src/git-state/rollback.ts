/**
 * Rollback flow: checkout prior version + create new monotonic version.
 * Phase 0: stub. Implemented in Phase 1.
 */

// TODO(phase4): implement rollback flow
export type RollbackStub = Record<string, never>;
