/**
 * Per-user and service token storage and refresh loop.
 * Phase 0: stub. Implemented in Phase 2.
 */

// TODO(phase2): implement token storage, refresh, and bridge to ~/.tentacular/tokens/
export type TokensStub = Record<string, never>;
