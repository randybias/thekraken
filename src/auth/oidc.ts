/**
 * Keycloak OIDC device authorization flow.
 * Phase 0: stub. Implemented in Phase 2.
 */

// TODO(phase2): implement device auth flow with 12-hour session window
export type OidcStub = Record<string, never>;
