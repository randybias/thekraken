/**
 * SlackBot: HTTP Events API + Socket Mode integration via @slack/bolt.
 * Phase 0: stub. Implemented in Phase 1.
 */

// TODO(phase1): implement SlackBot class
export type SlackBotStub = Record<string, never>;
