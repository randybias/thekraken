/**
 * POSIX-like authorization engine (mode bits, owner/group enforcement).
 * Phase 0: stub. Implemented in Phase 2.
 */

// TODO(phase2): port POSIX authz from thekraken-reference with adjustments
export type AuthzStub = Record<string, never>;
