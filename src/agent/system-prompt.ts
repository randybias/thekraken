/**
 * System prompt builder: skill + enclave MEMORY + persona + [CONTEXT] block.
 * Phase 0: stub. Implemented in Phase 1.
 */

// TODO(phase1): implement system prompt assembly
export type SystemPromptStub = Record<string, never>;
