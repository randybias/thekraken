/**
 * Per-thread concurrency queue.
 * Phase 0: stub. Implemented in Phase 1.
 */

// TODO(phase1): implement per-thread queue for agent request serialization
export type QueueStub = Record<string, never>;
