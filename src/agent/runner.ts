/**
 * Per-thread pi agent lifecycle manager.
 * Phase 0: stub. Implemented in Phase 1.
 */

// TODO(phase1): implement agent runner using pi-agent-core
export type AgentRunnerStub = Record<string, never>;
